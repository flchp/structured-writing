<div class="col-md-12">
  <div class="post">
    <%= link_to("写文章", new_account_post_path, class: "btn btn-primary") %>
  </div>
  <table class="table table-hover">
    <thead>
    <tr>
      <td>#</td>
      <td>文章题目</td>
      <td>发表时间</td>
    </tr>
    </thead>
    <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td>#</td>
        <td><%= link_to(post.title, account_post_path(post)) %></td>
        <td><%= post.created_at.strftime("%Y-%b-%d   %H:%M") %></td>
        <td>
          <%= link_to("修改", edit_account_post_path(post), class: "btn btn-sm btn-primary")%>
          <%= link_to("删除", account_post_path(post),    class: "btn btn-sm btn-danger",
                      method: :delete, data: { confirm: "Are you sure?" } )%>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>