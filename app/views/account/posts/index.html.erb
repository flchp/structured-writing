<div class="col-md-12">
  <div class="post">
    <%= link_to("New post", new_account_post_path, class: "btn btn-primary") %>
  </div>
  <table class="table table-hover">
    <thead>
    <tr>
      <td>#</td>
      <td>Title</td>
      <td>Created_at</td>
    </tr>
    </thead>
    <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td>#</td>
        <td><%= link_to(post.title, account_post_path(post)) %></td>
        <td><%= post.created_at.strftime("%Y-%b-%d   %H:%M") %></td>
        <td>
          <%= link_to("Edit", edit_account_post_path(post), class: "btn btn-sm btn-primary")%>
          <%= link_to("Delete", account_post_path(post),    class: "btn btn-sm btn-danger",
                      method: :delete, data: { confirm: "Are you sure?" } )%>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>