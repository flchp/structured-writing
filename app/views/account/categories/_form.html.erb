<%= simple_form_for [:account, @category] do |f| %>


  <%= f.input :name, label: "标签名字" %>

  <%= f.input :description, label: "标签内容"%>

  <%= f.submit "新增", class: "btn btn-primary", data: { disable_with: "Submitting..." } %>

<% end %>